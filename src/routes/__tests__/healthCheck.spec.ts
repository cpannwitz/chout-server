// import request from 'supertest'
// import getApp from '../../app'

describe('Login', () => {
  it('calls the check correctly', () => {
    return
    // done()
    // request(getApp())
    //   .get('/health')
    //   .end(function(err, res) {
    //     if (err) throw err
    //     expect(res.body).toMatchObject({
    //       status: 'OK',
    //       dbStatus: 'OK',
    //       redisStatus: 'OK'
    //     })
    //     done()
    //   })
  })
})
